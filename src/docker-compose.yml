services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: barcelonaxplorer_nginx
    ports:
      - "8080:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
